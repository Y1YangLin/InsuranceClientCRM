<IfModule mod_rewrite.c>
  Options -Multiviews
  RewriteEngine on

  # RewriteBase /clinico/public will reduce 500 internal server error
  RewriteBase /

  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule  ^(.+)$ index.php?url=$1 [QSA,L]


</IfModule>